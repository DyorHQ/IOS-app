# DyorHQ web/server environment (example — copy to .env and fill in). NEVER commit real secrets.

# IPFS pinning for Moment media (Pinata). PINATA_JWT is also used by scripts here, but the DEPLOYED Supabase Edge
# Function `pin-media` reads PINATA_JWT from the project's Edge Function SECRETS, not from this file — set it there:
#   Supabase dashboard -> project fmnjqrguvopusfufmirs -> Edge Functions -> Secrets -> add PINATA_JWT
PINATA_JWT=
PINATA_API_KEY=
